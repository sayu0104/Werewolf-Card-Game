# CLAUDE.md

このファイルはClaude Code起動時に毎回読み込まれる。目次として使い、詳細は各ドキュメントを参照する。

## プロジェクト概要
人狼ゲームとカードゲームを融合させたブラウザゲームアプリ。1人プレイ（AIキャラクターとの対戦）をメイン目標とし、複数人プレイにも対応する。

## ドキュメント一覧
- 要件定義書: `docs/requirements.md`
- データベース設計書: `docs/database-design.md`
- 開発サイクル・運用ルール: `docs/development-workflow.md`

## 応答スタイル（Claude Code向け）
- 日本語で答える。敬語・前置き・感想・お礼は書かない（「です・ます」禁止、常体で返す。詳細は末尾の「厳守ルール」参照）
- 体言止めを基本にする（「〜します」→「〜する」）
- 結論を最初に書く。理由は聞かれたときだけ
- 「承知しました」「〜してみます」等の相槌を書かない
- 同じ内容を言い換えて繰り返さない
- 変更した箇所だけ出す。ファイル全体を再掲しない
- コードの逐一説明はしない。必要な注意点だけ1〜2行
- 頼んでいない追加機能・リファクタ・改善提案はしない
- 作業前に長い計画を書かない。要点のみ
- エラー処理・入力チェック・セキュリティは省略しない
- 不明点は勝手に決めず、1行で短く確認する

## 技術スタック（想定）
- バックエンド: Java / Spring Boot
- DB: MySQL
- フロントエンド: 未確定（Thymeleafベースか、API+JS/フロントエンドフレームワークかは今後検討）

## 学習スタイル・開発方針
- コードの答えを直接提示するのではなく、自分で考えられるように誘導してほしい（ソクラテス式）
- 未確定事項が多いプロジェクトのため、実装前に `docs/requirements.md` の「未確定事項」セクションを都度確認し、
  仕様が固まっていない箇所は先に確認してから実装を進める
- ゲームロジック（コスト計算、被疑心ポイント、投票処理など）はルールが複雑なため、
  実装時は要件定義書の該当セクションを引用しながら会話する
- 開発サイクル・ブランチ運用・テスト方針は `docs/development-workflow.md` に従う

## 現状
- ドキュメント・GitHubリポジトリ・Spring Bootプロジェクト雛形・MySQL接続を整備済み
- コード実装はまだ未着手

---

## 厳守ルール（毎回、例外なく守ること）

### 応答スタイル
- ユーザーへの返信では敬語を一切使わない。常体（だ・である調、または簡潔な
  口語）で返す。「です・ます」を使わない。トークン節約のため、例外なく厳守する。

### 進捗管理
- Issue を1つ完了するたびに、docs/task-tracker.md を必ず更新する
  （完了項目を [x] に変え、Issue番号・PR番号を追記）。
  更新はコード変更とは別ブランチ・別PRで行う。

### ファイル報告
- 実装が終わったら、今回「新規作成したファイル」と「変更したファイル」の
  一覧を、それぞれ分けて必ず報告する。

### テストデータ
- 動作確認のためにテストデータをDBへ投入した場合、確認後は必ず削除して
  DBを元の状態に戻す。投入時は文字コード（UTF-8）を明示し、文字化けを防ぐ。

### push とマージの順序
- push は、そのブランチの作業をすべて終えてから行う。
- 一度マージしたブランチに、後から追加 push しない
  （main に変更が取り残されるのを防ぐため）。
